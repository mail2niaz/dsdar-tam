<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- the theme applied to the application or activity -->
    <style name="CustomActionBarTheme" parent="@style/Theme.AppCompat">

        <!-- <style name="CustomActionBarTheme"  parent="android:Theme.Light"> -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="android:actionBarTabStyle">@style/MyActionBarTabBar</item>
        <!-- <item name="android:actionBarItemBackground">@color/actionbar_bg</item> -->
        <item name="android:actionMenuTextColor">@android:color/black</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="actionBarTabStyle">@style/MyActionBarTabBar</item>
        <!-- <item name="actionBarItemBackground">@color/actionbar_bg</item> -->
        <item name="actionMenuTextColor">@android:color/black</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <!-- <item name="android:background">@drawable/nav_bg_i4</item> -->
        <item name="android:background">@color/black</item>
        <item name="android:actionBarSize">40dip</item>
        <item name="android:minHeight">40dip</item>
        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
        <!-- <item name="background">@drawable/nav_bg_i4</item> -->
        <item name="background">@color/black</item>
        <item name="actionBarSize">40dip</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
        The textColor property is backward compatible with the Support Library</style>

    <!-- ActionBar tabs text -->
    <style name="MyActionBarTabText" parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@android:color/white</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>

    <style name="MyActionBarTabBar" parent="@style/Widget.AppCompat.ActionBar.TabView">

        <!-- tab indicator -->
        <item name="android:background">@drawable/android_tab_indicator</item>

        <!-- Support library compatibility -->
        <item name="background">@drawable/android_tab_indicator</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>

    <style name="noAnimTheme" parent="android:Theme">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>